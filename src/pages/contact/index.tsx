import Styles from './Contact.module.scss';

export default function Contact () {
    return (
        <main className={Styles.contact}>
            <h1 className={Styles.contact__title}>Get in touch! Form and social media.</h1>
        </main>
    );
}